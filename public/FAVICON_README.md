# Favicon

Place your favicon image at:
  /public/aa.png

That's all — the website head already references /aa.png automatically.

To replace the favicon:
  1. Put your new image file in /public/
  2. Name it aa.png  (or update the href in src/routes/index.tsx)

Supported formats: .png, .ico, .svg
